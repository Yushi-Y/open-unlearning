defaults:
  - finetune

handler: EigenSurgery
args:
  optim: sgd
  learning_rate: 0.01
  lr_scheduler_type: constant
  gradient_accumulation_steps: 1

method_args:
  cfg:
    n_pcs_select: 200
    reg_eps: 1e-4
    surgery_alpha: 0.1
