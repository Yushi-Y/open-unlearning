# @package hydra.sweeper
params: 
  trainer.args.learning_rate: "tag(log, interval(0.05, 0.5))"
  trainer.method_args.cfg.retain_momentum: "interval(0.95, 0.99)"
  trainer.method_args.cfg.lora_lr: "tag(log, interval(1e-4, 5e-3))"
  # trainer.method_args.cfg.n_pcs: "tag(log, range(300, 500))"