# @package hydra.sweeper
params: 
  trainer.args.learning_rate: "tag(log, interval(0.1, 0.4))"
  trainer.method_args.cfg.n_pcs: "tag(log, range(200, 400))"
  trainer.method_args.cfg.retain_momentum: "interval(0.9, 0.99)"
  trainer.method_args.cfg.lora_lr: "tag(log, interval(1e-4, 5e-4))"

  # trainer.method_args.cfg.train_first_layers: "interval(0.7, 1)"
  # trainer.method_args.cfg.lora_rank: "range(16, 32)"
  # trainer.method_args.cfg.lora_decay: "interval(0.99, 1)"
  # trainer.method_args.cfg.loss: choice(logit_loss, neg_ce_loss)
