# @package hydra.sweeper
params: 
  trainer.args.learning_rate: "tag(log, interval(0.06, 0.4))"
  trainer.method_args.cfg.lora_lr: "tag(log, interval(0.3, 10))"
  trainer.method_args.cfg.retain_momentum: "interval(0.95, 0.98)"
  # trainer.method_args.cfg.n_pcs: "tag(log, range(300, 600))"