# @package hydra.sweeper
params:
  trainer.args.learning_rate: "tag(log, interval(0.05, 0.4))"
  trainer.method_args.cfg.lora_lr: "tag(log, interval(0.03, 3))"
  # trainer.method_args.cfg.retain_momentum: "interval(0.9, 0.99)"
  # trainer.method_args.cfg.n_pcs_select: "range(100, 400, step=100)"
