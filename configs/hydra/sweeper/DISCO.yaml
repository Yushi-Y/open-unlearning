# @package hydra.sweeper
params:
  trainer.args.learning_rate: "tag(log, interval(0.01, 0.2))"
  trainer.method_args.cfg.n_pcs_select: "range(50, 400, step=50)"
  trainer.method_args.cfg.reg_eps: "tag(log, interval(1e-6, 1e-2))"
